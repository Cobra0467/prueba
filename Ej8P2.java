import java.util.Scanner;
 
public class Ej8P2
{
    public static void main(String[] args)
    { 
        int numero 0;

        Scanner sc = new Scanner(System.in);

        System.out.println("Introduce un numero");

        numero = sc.nextint();

        String numCadena = String.valueof(numero);

        if (numero < 0 || numero > 9999) {
            System.out.println("El número debe ser entre 0 y 9999");
        }
        if (numero > 0 && numero <- 9) {
            System.out.println("No se puede poner al revés");
        }
        if (numero > 0 && numero <- 90) {
            char n1 - numCadena.charAt(0);
            char n1 - numCadena.charAt(1);
            System.out.println("" + n2 + n1);
        }
        else if
        }

    }


}